@model CretaceousClient.Models.Animal

<h1>Edit this Animals information</h1>

@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.AnimalId)

    @Html.LabelFor(model => model.Name)
    @Html.EditorFor(model => model.Name)

    @Html.LabelFor(model => model.Gender)
    @Html.EditorFor(model => model.Gender)

    @Html.LabelFor(model => model.Species)
    @Html.EditorFor(model => model.Species)

    @Html.LabelFor(model => model.Age)
    @Html.EditorFor(model => model.Age)

    <br>
    <br>

    <input type="submit" value="Save Details" />
}


@* EPICODUS REMOTE REPO *@
@* <form action="/Animals/Details/@(Model.AnimalId)" method="post">
  <label for="name">Animal's Name</label>
  <input id="name" name="name" type="text" value="@(Model.Name)">
  <label for="species">Animal's Species</label>
  <input id="species" name="species" type="text" value="@(Model.Species)">
  <label for="age">Animal's Age</label>
  <input id="age" name="age" type="number" value="@(Model.Age)">
  <label for="gender">Animal's Gender</label>
  <input id="gender" name="gender" type="text" value="@(Model.Gender)">
  <button type="submit">Go!</button>
</form> *@

  @* [HttpPost]
  public IActionResult Details(int id, Animal animal)
  {
    animal.AnimalId = id;
    Animal.Put(animal);
    return RedirectToAction("Details", id);
  } *@


@* TABLE TEMPLATE *@
  @* @using (Html.BeginForm("Put", "Animals"))
  {
    @Html.HiddenFor(model => model.AnimalId)
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Species</th>
                <th>Age</th>
                <th>Gender</th>
        </tr>
        </thead>
        <tbody>
                <tr>
                    <td>@Model.Name @Html.EditorFor(model => model.Name)</td>
                    <td>@Model.Species</td>
                    <td>@Model.Age</td>
                    <td>@Model.Gender</td>
                </tr>
        </tbody>
    </table>

    <input type="submit" value="Submit Edits" class="btn btn-default" />
  } *@